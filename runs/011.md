# Run 011 — Fresh discovery resumed, new upstream PR opened

**Date**: 2026-03-13
**Trigger**: Oak said "run."
**Prior state**: `runs/001.md` through `runs/010.md` existed. The archive had two live public artifacts: `jd/tenacity#630` and `date-fns/date-fns#4168`. Run 010 concluded that the old local backlog was exhausted and the next run should both monitor live PRs and restart fresh bug-report-driven search.

---

## What was attempted

Started by restoring continuity on the public artifacts, then moved into a fresh search because neither existing PR had immediate reviewer action.

### Step 1: Recheck the public frontier

Live state on 2026-03-13 before new work:

- **`jd/tenacity#630`** — open, no comments or reviews, merge state `CLEAN`, all GitHub checks passing
- **`date-fns/date-fns#4168`** — open, no comments or reviews, merge state `BLOCKED`, no checks attached

This confirmed Run 010's main point: there was nothing to repair immediately, so new discovery was the highest-value action.

### Step 2: Search for a fresh bug report with a narrow reproduction path

Scanned current open issues in several previously productive repositories, then focused on `dateutil/dateutil` because:

- the project had already yielded verified fixes in earlier runs
- setup cost was low
- the open issue list contained several parser / recurrence bugs with concrete repros

Selected **`dateutil/dateutil#1452`**:

- title: ``FREQ=YEARLY` with `BYMONTHDAY` but no `BYMONTH` expands to every month instead of `DTSTART` month`
- status: open
- comments: 0
- no matching PR found in `gh pr list`

### Step 3: Reproduce `dateutil#1452` on current upstream

Cloned current `dateutil/dateutil` `master` at:

- `c981f9c7aa91b83cc9bd33a09ecee9e751b06e8d`

Ran the issue's reproduction against the current code and confirmed the bug still existed:

```python
from datetime import datetime
from dateutil.rrule import rrule, YEARLY

dtstart = datetime(2010, 3, 30)
rr = rrule(YEARLY, bymonthday=30, dtstart=dtstart, count=12)
```

Observed output:

- `2010-03-30`
- `2010-04-30`
- `2010-05-30`
- ...

Expected output:

- `2010-03-30`
- `2011-03-30`
- `2012-03-30`
- ...

So the recurrence was still expanding across every month rather than using the `DTSTART` month.

### Step 4: Inspect the constructor logic and apply a minimal fix

The relevant logic in `src/dateutil/rrule.py` only inferred `bymonth = dtstart.month` for `YEARLY` rules when **no** `BYxxx` selectors were present. That excluded the `BYMONTHDAY`-only case from RFC-style month inference.

Applied a narrow fix:

- when `freq == YEARLY`
- and `bymonth` is missing
- and `bymonthday` is present
- and there is no `byweekday`, `bysetpos`, `byyearday`, `byweekno`, or `byeaster`

then infer `bymonth` from `dtstart.month`.

This was intentionally narrower than a blanket RFC reinterpretation. An initial broader patch caused `testYearlyBySetPos` to fail by collapsing valid year-wide selector behavior into a single month. Narrowing to the plain `BYMONTHDAY` case fixed issue `#1452` without regressing the existing selector combinations.

### Step 5: Add regression coverage

Updated and extended `tests/test_rrule.py`:

1. corrected the existing `testYearlyByMonthDay` expectation
2. added `testYearlyByMonthDayUsesDtstartMonth` covering the exact March 30 yearly recurrence from `#1452`

The existing `BYMONTHDAY + BYWEEKDAY` behavior was left unchanged after the first broader attempt proved that the project currently treats those compound selectors differently.

### Step 6: Validate locally

Created a local virtualenv with `uv` and installed the project plus dev dependencies.

Relevant verification:

- `.venv/bin/pytest tests/test_rrule.py -k 'YearlyByMonthDay or YearlyBySetPos'` — passed
- `.venv/bin/pytest tests/test_rrule.py` — passed (`562 passed, 1 xfailed`)

Also ran the full suite:

- `.venv/bin/pytest`

Result:

- unrelated failures in `tests/test_tz.py` because the local checkout did not contain `src/dateutil/zoneinfo/dateutil-zoneinfo.tar.gz`

This appears to be an environment / package-data issue rather than a regression from the recurrence change, because the failures were confined to timezone resource loading and not to `rrule` behavior.

### Step 7: Upstream the fix

Created fork:

- `oaksprout/dateutil`

Pushed branch:

- `fix-yearly-bymonthday`

Opened upstream PR:

- **`dateutil/dateutil#1471`** — `Infer BYMONTH from DTSTART for yearly BYMONTHDAY rules`

PR URL: https://github.com/dateutil/dateutil/pull/1471

Added the required towncrier fragment in a follow-up commit:

- `f448c5c` — `Infer yearly bymonthday from DTSTART month`
- `714294c` — `Add changelog for yearly bymonthday fix`

### Step 8: Snapshot public state after the new PR

Live public artifacts at end of run on 2026-03-13:

- **`jd/tenacity#630`** — open, `CLEAN`, all checks passing, no review activity
- **`date-fns/date-fns#4168`** — open, `BLOCKED`, review required, no checks or comments
- **`dateutil/dateutil#1471`** — open, `UNSTABLE` only because CI had just started; large GitHub Actions matrix and Appveyor / ReadTheDocs were all queued, no comments or reviews yet

---

## Restoration result

| Metric | Project | Invariant | Before | After | Outcome |
|---|---|---|---|---|---|
| Fresh bug discovery | `dateutil` | Yearly `BYMONTHDAY` without explicit `BYMONTH` should use `DTSTART` month | expanded across every month in the year | uses `DTSTART` month for the plain `BYMONTHDAY` case | **FIX APPLIED** |
| Upstream contribution | `dateutil` | Verified local restoration should become public artifact | no PR existed for `#1452` | PR `dateutil/dateutil#1471` opened | **PR OPENED** |
| Public frontier continuity | `tenacity`, `date-fns`, `dateutil` | archive should track real external state | only two live PRs tracked, one omitted from Run 010 | all three live PRs re-synced in archive | **STATE RESTORED** |

---

## What was learned

1. **The archive had a continuity gap.** `date-fns/date-fns#4168` was still live, but Run 010 stopped mentioning it. Public artifacts need to be tracked as a set, not one at a time.

2. **Fresh bug-report-driven search still works after the old backlog expires.** The archive did not need another historical target to make progress; a current issue tracker search yielded a new PR in the same run.

3. **RFC-backed fixes still need behavioral containment.** The first reading of the spec suggested a broader `BYMONTH` inference rule, but the project's existing `BYSETPOS` / `BYWEEKDAY` behavior made that too disruptive. The right move was the smallest fix that resolved the demonstrated bug without rewriting adjacent semantics.

4. **Legacy tests can encode partial rather than universal truth.** One existing yearly `BYMONTHDAY` test encoded the buggy behavior for the plain case, while other compound-selector tests encoded currently intended broader behavior. The lesson is not just "tests can be wrong" but "tests may be right for one slice and wrong for a neighboring slice."

5. **Public artifact count matters for legitimacy.** The archive now has three externally visible PRs across three projects:
   - `date-fns/date-fns#4168`
   - `jd/tenacity#630`
   - `dateutil/dateutil#1471`

6. **Full-suite failures need classification, not concealment.** Running the whole `dateutil` suite still produced useful signal even though it was not clean: the failures were clustered in missing timezone package data, which bounded the uncertainty around the `rrule` change.

---

## Cross-run pattern update

New confirmed patterns:

- **Fresh search remains productive after backlog decay** — old archived fixes were exhausted, but a new issue search still produced a contribution-worthy fix
- **Spec-driven fixes often need scope control** — standards text can identify the bug, but neighboring selectors may still require a narrower implementation boundary
- **Public artifact tracking must be explicit** — once multiple upstream PRs exist, the archive needs to snapshot all of them each run

---

## What the next run should do

Priority order:

1. **Monitor CI and review on `dateutil/dateutil#1471`.** The new PR has a large queued matrix and will likely generate the highest-signal maintainer feedback.

2. **Continue monitoring `jd/tenacity#630` and `date-fns/date-fns#4168`.** Both remain open and could require follow-up even though they were idle during this run.

3. **Run another fresh issue search only if the public frontier remains idle.** There is now a three-PR maintenance surface; expansion should continue, but not at the cost of dropping live artifacts.

---

## State after this run

- `runs/001.md` through `runs/010.md` still exist
- `runs/011.md` exists (this file)
- three live public PRs now exist:
  - `date-fns/date-fns#4168`
  - `jd/tenacity#630`
  - `dateutil/dateutil#1471`
- local upstream checkout used for this run exists at `.context/dateutil`
- local branch in that checkout: `fix-yearly-bymonthday`
- pushed commits on fork branch:
  - `f448c5c`
  - `714294c`
